{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "event-v1.schema.json",
  "title": "Evento de progresso/estado (EVENTS-AND-PROGRESS)",
  "type": "object",
  "required": ["seq", "ts", "kind", "correlationId"],
  "properties": {
    "seq": { "type": "integer", "minimum": 0 },
    "ts": { "type": "string", "format": "date-time" },
    "kind": { "enum": ["job.progress", "job.state", "session.state", "session.environment", "session.resume", "entity.changed", "alert"] },
    "jobId": { "type": "string" },
    "sessionId": { "type": "string" },
    "gameId": { "type": "string" },
    "correlationId": { "type": "string" },
    "progress": {
      "type": "object",
      "properties": {
        "stage": { "type": "string" },
        "current": { "type": ["number", "null"] },
        "total": { "type": ["number", "null"] },
        "unit": { "enum": ["bytes", "items", "steps"] },
        "rate": { "type": ["number", "null"] },
        "currentItem": { "type": ["string", "null"] }
      }
    },
    "state": { "type": "string" },
    "digest": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "changes": { "type": "array", "items": { "type": "string" } },
    "suspendedSeconds": { "type": "number", "minimum": 0 },
    "error": { "$ref": "error-v1.schema.json" }
  },
  "additionalProperties": false
}
