# Relatório G26 — Correções do PR #21 (fix/test-state-isolation-g26)

## Resumo

Quatro das cinco correções solicitadas estão implementadas e verificadas.
A quinta (regeneração do relatório) é esta entrega. Nenhum commit foi criado;
as alterações permanecem na árvore de trabalho da branch
`codex/fix-test-state-isolation-g26` contra `origin/main@6e253f0`.

## Itens entregues

| # | Correção | Estado | Onde |
|---|----------|--------|------|
| 1 | Isolar HOME além das cinco XDG | ✅ | `isolated_environment()` em `tools/run_tests_isolated.py:90` define HOME como subdir do root; `tests/conftest.py:33` faz o mesmo via fixture session; `tests/conftest.py:61` restaura via monkeypatch por teste |
| 2 | Isolar antes da coleta (não só por teste) | ✅ | `test_direct_pytest_session_isolation` em `tests/unit/test_test_state_isolation.py:152` prova que um subprocesso com conftest local + fixture session-scoped autouse isola HOME + XDG antes de qualquer importação de módulo de teste |
| 3 | Auditoria de snapshot em `finally` (mesmo com interrupção) | ✅ | `tools/run_tests_isolated.py:198-211` — `finally` externo captura `KeyboardInterrupt` e `SystemExit`, executa snapshot after, `finally` interno com `contextlib.suppress` garante `temporary.cleanup()`, depois levanta `SystemExit(86)` se houve mutação |
| 4 | Entrypoints via `make -n` real (não substring em Makefile) | ✅ | `test_canonical_entrypoints_use_isolated_runner` em `tests/unit/test_test_state_isolation.py:108` executa `make -n test` e `make -n cov` e verifica `tools/run_tests_isolated.py` na saída |
| 5 | Relatório de `git diff origin/main...HEAD` | ✅ | Este documento |

## Modificações

### Arquivos modificados (tracked)

```
.github/workflows/ci.yml        |  4 ++--
 AGENTS.md                       |  2 +-
 Makefile                        |  4 ++--
 docs/WORKLOG.md                 | 26 ++++++++++++++++++++++++++
 tests/unit/test_switch_media.py |  5 +++++
 5 files changed, 36 insertions(+), 5 deletions(-)
```

### Arquivos novos (untracked)

- `tests/conftest.py` — fixture session `isolated_xdg_root` que cria tree temp com HOME + 5 XDG; fixture autouse `enforce_xdg_for_each_test` que restaura vars por monkeypatch
- `tests/unit/test_test_state_isolation.py` — 8 testes de regressão para isolamento de estado
- `tools/run_tests_isolated.py` — runner canônico com snapshot antes/depois, auditoria em `finally`, exit 86 em mutação

### Fora de escopo

- `wheelhouse/` — artefatos de release (regra §4)
- `docs/diagnostics/2026-07-23-catalogo-falhas-emulacao.md` — diagnóstico anterior, não relacionado

## Gates

| Gate | Resultado |
|------|-----------|
| `ruff check` | ✅ limpo |
| `mypy src`  | ✅ 188 arquivos |
| `make independence` | ✅ OK |
| `make boundaries` | ✅ 0 violações |
| Testes de unidade | ✅ 2209 passaram em 142s |

## Pendências

- **Commit**: as alterações não estão commitadas. O operador revisou e está
  satisfeito (`"Vamos para o commit"`). Pendente de `git add -A` + `git commit`.
- **Push**: pendente de autorização do operador.
