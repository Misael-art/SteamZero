# KNOWN-GAPS — lacunas conhecidas e classificadas

> Este registro mantém a história e a classificação de riscos. O estágio atual
> de uma capacidade está em [`docs/STATUS.md`](STATUS.md); itens de status devem
> apontar para `GAP-*` quando a lacuna ainda condiciona sua próxima ação.

| # | Lacuna | Classificação | Mitigação planejada |
|---|---|---|---|
| G1 | `RetroDECK/components` não clonado integralmente (timeout por blobs pesados; 5.516 paths só em `archive_later/`) | **Análise parcial** — modelo compreendido por amostragem (framework + duckstation + es-de) | Clonar com `--filter=blob:none --sparse` em máquina com rede melhor antes da Fase 4 (adapters) |
| G2 | EmuDeck: apenas ramo principal analisado; `android/`, `chimeraOS/`, `darwin/` não auditados em profundidade | **Fora de escopo v1** (alvos não-Linux-desktop) | Registrar; revisitar se houver demanda |
| G3 | PhaseZero lado Windows (`bootstrap-tools.ps1`, ~16k linhas) auditado por documentação (`CLAUDE.md`) e amostragem, não linha a linha | **Análise dirigida** — padrões de resiliência (checkpoint, change manifest, rollback, disk guard) confirmados pela doc interna e testes | O Unified é Linux-first; o lado Windows serve como referência conceitual, não como fonte de código |
| G4 | Nenhum teste dinâmico executado (nenhum script dos quatro projetos foi rodado) | **Restrição da Fase 0** (proibido mutar estado) | Fase 1+: bancada de testes com injeção de falhas |
| G5 | Nenhuma validação em hardware Steam Deck (LCD/OLED/dock) | **Bloqueador para "READY FOR IMPLEMENTATION" pleno** da parte Deck-específica | Matriz §13.4; depende de Q6 (OPEN-QUESTIONS) |
| G6 | Comportamento real do cloud sync EmuDeck (rclone) não exercitado; análise só estática de `cloudServicesManager.sh`/`cloudSyncHealth.sh` | Análise parcial | Prototipagem descartável na Fase 3 |
| G7 | Licenças de assets (ícones, artes, templates de controle) dos projetos de referência não inventariadas item a item | Pendência legal | 11-legal/THIRD-PARTY-NOTICES lista os blocos conhecidos; inventário fino antes de redistribuir qualquer asset |
| G8 | Versões exatas de SteamOS/Steam Client para a matriz de compatibilidade (§11.5) não levantadas | Pendência de pesquisa operacional | Levantar na Fase 2 com hardware real |
| G9 | PhaseZero não tem arquivo LICENSE | Pendência legal (Q2/Q3) | Decisão do titular antes de qualquer reuso formal |
| G10 | Requisitos de acessibilidade (narração/screen reader em Game Mode) sem solução técnica validada em Godot | Pesquisa pendente | ADR-0002 exige protótipo com critérios mensuráveis na Fase 5 |
| G11 | Boot direto/autologin da sessão Game Mode não validado com snapshot+console de recuperação | Bloqueio operacional G7 | Sessão manual SDDM e fallback Plasma existem; não tocar em GRUB/SDDM default antes do protocolo físico |
| G12 | Escala de texto do host (`forceFontDPI` do Plasma) não é honrada pela UI | **Lacuna de acessibilidade** — `reducedMotion` e `highContrast` já vêm do host via `dashboard.accessibility`; a escala não | O QML fixa `font.pixelSize` em ~72 pontos só no `Main.qml`, mais `Emulation.qml` e `SteamGameplay.qml`; aplicar escala exige um helper de tipografia e reexecução dos harnesses de layout/foco. Não foi feito junto do alto contraste para não misturar mudança de layout com mudança de cor às vésperas de uma release |
| G13 (QML-HARNESS-MIGRATION) | Dez harnesses QML antigos (`check_handheld_shell`, `check_main_emulation`, `check_emulation`, `check_steam_gameplay_responsive`, `check_handheld_layout_focus`, `check_main_handheld_sections`, `check_credentials`, `check_credential_dialog_responsive`, `check_high_contrast`, `check_packaged_assets`) ainda rodam sob `qml6` com `skipif` | **Legado tolerado, não é gate** — um `skip` produz suíte verde num host onde nada visual foi verificado, que foi como a regressão de ícones da a37 atravessou | Convertê-los para o harness próprio do projeto depois do VS-03. Enquanto não converterem: não contam como gate, e nenhum teste novo da vertical slice pode usar `skip` (o `check_scene_text` já reprova com `QML-VISUAL-ENVIRONMENT-001`). O job `qml-visual-linux` do VS-03 reprova quando Qt ou captura estiverem indisponíveis |
| ~~G14~~ | ~~Baselines visuais não versionadas: o texto usava a Liberation Sans do SISTEMA~~ | **FECHADA em 2026-07-28** (`1d9a52e` + `6fb2a75`) | A causa era mais funda que a suposta: não bastava empacotar a fonte, porque a do SISTEMA sombreava a empacotada mesmo com o arquivo certo carregado (`contentWidth` 320.08 → 323 ao isolar). O harness passou a isolar o fontconfig na fixture, e as dez baselines foram geradas nesse ambiente |
| G15 | `accessibilityGeneration` existe e está mapeada em `GENERATION_BY_KIND`, mas nenhuma propriedade real a consome | **Infraestrutura pronta, consumidor ausente** — a invalidação seletiva por perfil de acessibilidade não pode ser declarada como gate funcional enquanto nada depende dela; um consumidor artificial criado só para o teste passar provaria apenas que o teste passa | Fechar junto da entrega de acessibilidade (`textScale`, `reducedMotion`, `highContrast` consumidos por token ou setting real). Até lá: `accessibilityGeneration infrastructure: ready`, `accessibility-dependent property: not implemented`, `selective accessibility invalidation: deferred` |
| ~~G16~~ | ~~`test_cli_emulation.py::test_controls_cli_plan_apply_status_and_rollback` reprova de forma intermitente no CI~~ | **FECHADA em 2026-07-29** — o CI gerou legitimamente `confirmToken=-zfAF68ralrhqGIdv1zKbFSCRDyofMsy`; `_validate_controls_args` confundia o hífen inicial permitido por `token_urlsafe` com uma flag. Não era expiração nem colisão XDG | `--confirm` agora aceita o alfabeto opaco produzido pelo próprio backend, sem relaxar os demais valores de controls. A regressão força deterministicamente o token exato que reprovou no CI e percorre plan → apply → status → rollback |
| ~~G17~~ | ~~`steamzero service status --json` não existe — devolve `E-CLI-USAGE: ação desconhecida`~~ | **FECHADA em 2026-07-29** — o comando público observa `current`, identidade do daemon e quarentena sem atravessar o próprio daemon | O observer não recebe callback de restart nem executa retry: publica `converged`, `pending`, `timeout` ou `unreadable`. Testes provam drift, daemon ausente, `current` ausente e zero reinícios |
| ~~G18~~ | ~~Rollback para release anterior ao HOST-ACTIVATION-01 deixa daemon defasado sem gate~~ | **FECHADA em 2026-07-29 pela a39** | O gerenciador estável fora de `current` executou fisicamente `a39→a37→a39`: em cada direção observou o daemon stale, convergiu em 1 tentativa e foi idempotente na repetição (0 tentativas, mesmo PID). A a37 foi verificada por versão, PID e `/proc/<pid>/exe`; a a39 por identidade completa. Evidência em `A39-CERTIFICATION-RESULT.md` |
| ~~G19~~ | ~~Códigos `E-HOST-*` do HOST-ACTIVATION-01 não registrados no catálogo de erros~~ | **FECHADA em 2026-07-29** | Os cinco códigos estão no catálogo autoritativo e no i18n pt-BR. Testes parametrizados percorrem `build_error` e o envelope de `service refresh --expect-release` para cada diagnóstico; a lógica de convergência não mudou |
| ~~G20~~ | ~~`emulation workspace` nunca lê o estado real do host~~ | **FECHADA em 2026-07-29** | A CLI passou a reusar `EmulationController.snapshot()` em vez de manter uma segunda composição parcial. Medido no host: jogos **0 → 15**, keys **`unverified` → `ok` (rev21)**, firmware `ok` (22.5.0). `truthState` segue `unverified` porque nenhum emulador está instalado — estado honesto, não defeito. O gate contra reincidência verifica o AST do handler: `build_switch_workspace` não pode ser CHAMADO ali |
| G21 | 46 warnings QML por sessão vindos do estilo Breeze do KDE | **P3 — ruído, não defeito nosso** | Todos de `qrc:/qt/qml/org/kde/breeze/*`; nenhum de QML do SteamZero. Polui a coleta de warnings próprios na UI real. Filtrar por origem ao coletar, sem silenciar globalmente |
| ~~G22~~ | ~~`test_10k_fixture_apply_and_rollback_benchmark` afirmava tempo de parede (`< 180 s`) em runner compartilhado~~ | **FECHADA em 2026-07-30** — o teto absoluto foi removido; o tempo virou medição publicada via `--durations=20` + JUnit XML no CI | As asserções funcionais de 10 mil moves, rollback e staging limpo permanecem. Não há mais teto absoluto em runner compartilhado. O JUnit XML por versão Python preserva a duração real para consulta sem rerun. Uma regressão de performance só poderá voltar a bloquear por comparação controlada no mesmo ambiente ou runner dedicado |
| ~~G23~~ | ~~`test_daemon_controls_profile_roundtrip_is_closed_and_reversible` falhou uma vez no Python 3.12 porque a leitura imediata após `apply` retornou `active=None`~~ | **FECHADA em 2026-07-29** — o gatilho isolado não foi atribuído; o defeito comprovável era o teste descartar `state`/`detail` e `status()` colapsar erro de leitura em ausência | 50 servidores locais e cinco execuções por Python 3.11/3.12/3.14 ficaram `ready`. A contaminação XDG mais ampla encontrada depois é **G26**; ela confirma que o risco de isolamento era real, sem reabrir ou reatribuir retroativamente G23 |
| ~~G24~~ | ~~`_requirement_payload` degrada em silêncio quando o mapeamento de requisito é parcial~~ | **FECHADA em 2026-08-02 (REQUIREMENTS-E2E)** — dado parcial agora gera diagnóstico em vez de degradar calado: o valor informado é **preservado** (visível na UI), o status continua honesto (`unverified`: parcial não é confiável), e o `detail` nomeia exatamente as chaves que faltam — o usuário distingue "faltou informação" de "sumiu". A posição no contrato segue autoritativa (firmware rotulado como keys nunca é aceito). Testes: `TestAPartialRequirementIsDiagnosed` em `test_emulation_workspace_cli.py` (preservação + kind autoritativo) |
| ~~G25~~ | ~~Dois jobs `media.global` sobrevivem desde 2026-07-26 como `running`; `JobManager.recover()` existe, mas não é chamado no bootstrap; doctor continua `ok` e cancelamento sem runner fica inerte~~ | **FECHADA em 2026-07-31** — recovery no bootstrap (`_recover_at_boot`: `JobManager.recover()` + `transaction.recover_all()`); `recover()`/`cancel()` levam jobs stalados a terminal (sem reenfileirar); doctor ganha 4 checks de auditoria (`jobs.stale`, `staging.orphan`, `backup.orphan`, `journal.orphan`); novos comandos `state audit` + `cleanup-plan`/`cleanup-apply` (quarentena recoverable). Evidência: `docs/diagnostics/2026-07-31-g25-bootstrap-recovery-evidence.md` |
| ~~G26~~ | ~~Suítes transacionais escrevem journal, planos e backups no XDG real; state home chegou a ~1,1 GB e contém artifacts sem referência no banco~~ | **FECHADA em 2026-08-01 (PR #21)** — `tests/conftest.py` (commit `08331ab`) define os cinco homes XDG temporários + `HOME` no carregamento do módulo, com fixture autouse por teste e gate before/after (`STEAMZERO_TEST_XDG_ROOT`); o runner canônico `tools/run_tests_isolated.py` (commit `95f6ba5`) rejeita invocação fora do isolamento e o estado real do host permanece intocado nas execuções (verificado nos gates) | O acervo histórico (~1,1 GB) não foi limpo — exige plano e autorização do operador, ainda em aberto |
| ~~G27~~ | ~~CLI, workspace e QML divergem no lifecycle: Citron AppImage está `degraded` por drift, a UI o chama de "não instalado", `component list` usa Flatpak e a prontidão ignora o default degradado~~ | **FECHADA em 2026-08-01 (PR #24, merge `83cf7e4`)** — fachada `ComponentLifecycle` (`src/steamzero/adapters/lifecycle.py`) roteia pelo executor da fonte declarada; status normalizado com `degraded` preservado e falha agregada por componente; planos v2 executor-independentes validados (schema fecha executor↔delegado, ULID, token ASCII, timezone); `launch()`/`stop()` roteiam EOL Flatpak como Flatpak; dashboard e QML tratam degradado/EOL/unavailable sem colapso e respeitam `installable` | Release `0.1.0a41-c9111a00d3c0` (contém G27/G28) instalada no host em 2026-08-01, daemon convergido; **aguarda reboot e teste físico** (boot direto, GUI) — teste exige reinicialização pelo operador |
| ~~G28~~ | ~~Refresh de mídia processa 15 jogos por ~177 s, atualiza zero e persiste quota excedida, mas workspace/UI publicam `providerErrors: {}`~~ | **FECHADA em 2026-08-01 (PR #26)** — causa raiz: `StateStoreGameMediaAdapter.save()` nunca persistia `GameMediaState.errors` (e `_row_to_state` nunca lia), então a falha remota sumia após restart. Fix: migração m0014 (`errors_json` em `switch_game_media`) + m0015 (`scraping_provider_status` ganhou `last_error_code`/`last_error_category`/`state`); health persistida é sanitizada (nunca credenciais/detalhes/URLs — só códigos, categorias estáveis e contadores); contrato terminal do job `media.global` (`outcome` success/partial/degraded, `provider_errors`, `provider_details`, `interrupted_providers`, `no_candidates`) persistido em `job.result`; circuit breaker de quota (1 tentativa ScreenScraper por job; providers saudáveis seguem); workspace reconstrói `providerDetails` da health após restart e `_enrich_games` emite `mediaErrorCategories` por jogo; QML ganhou card `media-provider-health` e `taskResultSummary` PT-BR | **Release `0.1.0a41-c9111a00d3c0` (contém G27/G28) instalada em 2026-08-01, daemon convergido; aguarda reboot e validação física** (refresh real com quota, UI) — exige reinicialização pelo operador |
| ~~G29~~ | ~~GameMode é publicado como pronto pela presença do binário, embora governor, `split_lock` e ioprio tenham sido recusados e o usuário não esteja no grupo exigido pela policy~~ | **FECHADA em 2026-08-01 (PR #27)** — verdade observada do Feral GameMode em seis dimensões (binário, daemon, autorização, capability, atividade) + efeitos individuais (governor, split lock, ioprio), derivada por probe read-only injetável que nunca altera o host; idle ≠ falha; parcial é degradado listando efeitos recusados; falha de sondagem é `unknown`, nunca falso verde; rótulos PT-BR por estado; plano administrativo declarativo (`gamemode-admin-plan-v1`) validado antes de publicar, sem endpoint de aplicação; CLI `desktop gamemode-status` e UI "Ver instruções" sem botão de mutação | **Não instalado no host** — host segue na release `0.1.0a41-c9111a00d3c0` (G27/G28) aguardando reboot; validação física (GameMode real no boot direto) exige release construída da main e autorização do operador |
| G30 | Escopo systemd atingiu 5,7 GB/662 MB de swap agregando UI e emuladores; não existe atribuição por UI, job e filho | **P1 — gate de recursos não atribuível** | Medir PSS/lifecycle por classe de processo sem persistir command lines ou paths privados; não chamar o agregado de vazamento |
| ~~G31~~ | ~~Quatro processos `qml6` terminaram em `SIGABRT` durante gates e `check_runtime_version()` não rejeita retorno não zero de `qml6 --version`~~ | **FECHADA em 2026-08-02 (PR #29 + PR #30)** — PR #29 introduziu probe que classifica timeout/sinal/código de saída como estados distintos e rejeita processo abortado; PR #30 fechou as divergências contrato×código que a revisão de IA expôs: (a) guard baseline/delta de coredump wired de verdade em `capture()` (antes só existia no código, sem caller — verde falso escondendo coredump), via `Popen` para capturar o PID do harness; (b) `OSError` (binário ausente/inexecutável) ganhou diagnóstico próprio `DIAG_QT_RUNTIME-020` em vez da mensagem enganosa "saiu com código None"; (c) timeout com `stderr=None` normaliza para vazio em vez do literal "None"; (d) `verify_packaged_qml()` tipado como `PackagedQmlStatus` (dataclass frozen) em vez de `dict[str, Any]` de chaves mágicas | Validação física (gate visual real no boot direto) exige release construída da main e reinicialização pelo operador |
| ~~G32~~ | ~~Dois testes do cast engine IPC (`test_start_session_already_running`, `test_pause_resume_with_pipeline`) flakearam em runs integrais sem causa raiz comprovada~~ | **FECHADA em 2026-08-11** — causa raiz comprovada e corrigida (commit `2a5c1df`, origem `0ae3702`): a barreira `start_done` só era liberada no caminho de commit e o backlog do listener (`LISTEN_BACKLOG=128`) mascarava o enfileiramento; agora a barreira é liberada em todo caminho terminal (commit ou aborto) e `accept()` só encerra em erros fatais, com contrato intercalado por `type`. O teste concorrente (`test_concurrent_start_sessions_single_session`) reprova no engine pré-correção (verificado com `stash`) e a tríade (`test_start_session_already_running` + `test_pause_resume_with_pipeline` + `test_concurrent_start_sessions_single_session`) rodou **50/50 iterações verdes** em 2026-08-11; `test_cast_engine_ipc.py` integral com 49 testes passed | Nenhum `sleep`, retry cego, skip ou `xfail` foi adicionado nem timeout aumentado; a prova em dois dispositivos LAN reais segue pendente de hardware |
| ~~G33~~ | ~~Gate canônico saía 86 (`E-TEST-REAL-STATE-MUTATED`) na `main` com todos os testes passando, de forma intermitente~~ | **FECHADA em 2026-08-03** — **não era vazamento da suíte**: o isolamento do G26 está intacto. O autor das escritas é o **daemon instalado do host** (`steamzero-core --systemd`), que roda com `HOME` real e sem `XDG_STATE_HOME` e portanto resolve para o MESMO state home que o guard fotografa. O reconciliador (`service/reconciler.py`) grava `session.environment.changed` a cada flap de rede/energia: `state.db` + `logs/core.jsonl`, e o `ensure_dir` do `AppendWriter` (`core/fs.py`) faz `chmod` incondicional que bumpa o **ctime** do diretório `logs` — exatamente a assinatura `logs, logs/core.jsonl, state.db`. Como os flaps são irregulares (2 a 18 min de intervalo medidos), janelas longas quase sempre pegavam um, e curtas às vezes não: daí a intermitência. Prova: fotografando o state real por 6 min **sem pytest algum**, seis mutações com a assinatura idêntica. Correção: o runner passou a **atribuir** a mutação — varre `/proc` por processos steamzero fora do isolamento que resolvam para o mesmo state home e separa donos externos anteriores à janela (diagnóstico `W-TEST-REAL-STATE-EXTERNAL-WRITER`, gate não reprova) de processos nascidos dentro dela (86, prováveis vazamentos). Nenhum caminho entrou em lista de exceções | Enquanto um dono externo está ativo, a atribuição é **degradada**: uma escrita da própria suíte ficaria encoberta por ele. O guard diz isso na própria mensagem e recomenda rodar com o daemon parado para rigor total. Comando `steamzero` curto do operador pode terminar entre duas amostragens e não ser nomeado — a mensagem do 86 lista esse falso positivo como primeira hipótese |
| ~~G34~~ | ~~`test_desktop_ui_bridge.py::test_status_keeps_full_emulation_model_across_http_thread` reprova de forma intermitente no **CI Python 3.11** com `TimeoutError` no teto de parede de 3 s do cliente em loopback~~ | **FECHADA em 2026-08-04 pelas PRs #49 + #50** — a lacuna foi registrada medindo `main` e `codex/fix-state-guard-attribution`, e **nenhuma das duas continha as #49/#50**: a correção já existia e ninguém tinha verificado. A causa nunca foi o teste. `/status` compõe o snapshot inteiro da dashboard e custava **3,3–3,75 s** contra teto de **3 s** — margem **negativa**, ou seja, o teste passava por sorte. A #50 (`lru_cache` nos registries bundled: `adapters/registry.py`, `domain/platforms.py`) removeu o custo; a #49 subiu o teto para 10 s. Medição própria de 2026-08-04, mesmo host: em `origin/main` (teto 3 s) o teste **reprova localmente sem carga alguma**, com a chamada em 3,01 s (bate no teto); na integração `main`+#49+#50 (`05f2f0b`) custa **1,58 s** contra teto de 10 s (~6,3× de margem). CI: **10 execuções serializadas** sobre `05f2f0b`, **10/10 verdes no Python 3.11** (runs 30892569708, 30892960248, 30893358083, 30893785407, 30894184615, 30894558905, 30896242288, 30896755389, 30897145962, 30897585693; 2 execuções canceladas por disparo concorrente foram descartadas, não contadas como verdes). Se a taxa de ~1 em 3 ainda valesse, 10 verdes seguidas teriam ~1,7 % de chance de sair por acaso. O job 3.11 também caiu de ~10 min para ~4,8 min | Nenhuma linha do teste foi alterada para fechar: fechou por correção de custo em produção, não por afrouxar asserção. **A closure só vale quando #49 e #50 forem mergeadas** — enquanto elas estiverem abertas, `main` continua com teto de 3 s e o flake. **Dívida remanescente: ver G35** |
| G35 | `request_json` (`tests/integration/test_desktop_ui_bridge.py`) ainda afirma tempo de parede absoluto (`timeout=10`) em runner compartilhado — mesma classe de defeito que fechou a G22 e que produziu a G34 | **P3 — não morde hoje, a classe permanece** | Com custo medido de 1,58 s contra teto de 10 s a margem é ~6,3×, então não há urgência; o registro existe para que a próxima manifestação não seja investigada do zero. Corrigir na classe, como a G22 foi: remover o teto absoluto e publicar a duração via `--durations` + JUnit XML, ou — mais forte — sincronizar o handshake do servidor HTTP da fixture, de modo que o teste não dependa de tempo de parede nenhum. **Aumentar o timeout de novo não conta como correção** |
| ~~G36~~ | ~~Card de sistema da biblioteca editorial ficava com 148 px onde o contrato reserva 192~~ | ~~P2 — rótulo de estado encostava na borda com 3+ sistemas~~ | Fechado: a causa era a geometria intrínseca do `Button` delegado (`implicitHeight=148`) prevalecendo sobre o `Layout.preferredHeight=192` da célula. O card agora publica `implicitHeight >= systemCardHeight`, além dos limites de layout. O harness mede card, mínimo, preferido, grade e linha em 3/37 sistemas, paisagem/retrato, rótulo longo, alto contraste e escala 200%. G25 histórico continua reservado ao recovery de jobs. |
| ~~G37~~ | ~~O preview do editor de temas não percorre a cadeia `extends`~~ | **FECHADA em 2026-08-11 (Fase 6)** | `_make_resolved` passou a resolver via `ThemeResolver`, com `_make_resolved_leaf` como degradação. Medido nos builtins: `extends=aura` agora dá `accent #22d3ee` / `background #0b1020`, e `extends=steamdeck` dá `#1b9e4a` — antes os três davam a paleta padrão. Onze testes, incluindo o que compara bases entre si: uma asserção de cor isolada passaria mesmo com a herança desligada, porque o fallback devolve o padrão |
| ~~G38~~ | ~~`doctor` `service.generation` passava só por ausência de quarentena, sem comparar `/opt/steamzero/current` com o daemon~~ | **FECHADA em 2026-08-11** — `service.generation` usa `release_convergence.observe()` read-only; `pending`/`mismatch` falham com releases; `timeout`/`unreadable` viram warn; nunca reinicia daemon | Testes `test_doctor_service_generation_*` com current=a44/daemon=a42 |
| G39 | Cadeia `extends` acima de `MAX_EXTENDS_DEPTH = 2` degrada para a paleta padrão sem avisar | **P2 — mesma forma do G37, um nível acima** | Descoberto ao testar a Fase 6. A sessão do editor já ocupa o nível 0, então um tema de usuário que estenda OUTRO tema de usuário chega ao nível 4 e é recusado pelo resolver; `_make_resolved` degrada e o usuário vê a paleta padrão sem nada indicando que a cadeia foi longa demais — exatamente o sintoma que o G37 corrigiu. Congelado por teste. Fechamento: propagar um diagnóstico de cadeia recusada até o preview, em vez de degradar calado |
| G40 | ScreenScraper nunca busca mídia: `jeuInfos.php` responde **HTTP 400 `Champ systemeid obligatoire si aucun CRC`** | **P2 — provedor inteiro inerte, com credencial válida** | Medido em 2026-08-12 contra a API real, com devid/devpassword do operador configurados. `_build_params` envia `romnom` e `romtype`, mas nunca `systemeid` (o id de sistema do ScreenScraper) nem hash de ROM — e a API exige um dos dois. Falta a tabela `platform_slug` → `systemeid`, que não existe no repositório e não deve ser adivinhada. Agrava: o cliente classifica HTTP 403 como `E-SCRAPE-QUOTA-EXCEEDED`, mas o servidor responde `Erreur de login` — a mensagem manda investigar cota quando o problema é credencial ou parâmetro. Fechar exige a tabela de systemeid versionada e a reclassificação do 403 |
| ~~G41~~ | ~~Cast LAN nunca descobre receptor porque o orquestrador não é configurável pela UI~~ | **FECHADA em 2026-08-12 — e o enunciado estava impreciso** | Não faltava configuração: faltava INSTÂNCIA. `self._cast` só recebia valor por injeção, usada apenas nos testes; nenhum caminho de produção construía um `CastOrchestrator`, então toda chamada via `None`. Medido: construir custa 0,003 s e não exige argumento nenhum. Agora nasce sob demanda em `_cast_orchestrator()` — preguiçoso, e não no `__init__`, porque construir dispara `_reconcile`, que encerra sessões órfãs dos providers, e isso não pode ser efeito colateral de instanciar o dashboard. Falha de construção degrada para indisponível em vez de derrubar a central. Medido depois da correção: `cast_status` sai de `unavailable/Orquestrador não configurado` para `idle/Nenhuma sessão ativa`, e `cast_discover` devolve 1 receptor (`local-browser`, protocolo `web-receiver`). Quatro testes existentes reprovaram porque CONGELAVAM o defeito — afirmavam indisponibilidade construindo o dashboard sem injeção, que era a produção quebrada; foram reescritos para a premissa certa (indisponível quando CONSTRUIR falha). Pareamento em receptor real continua por exercitar |
| ~~G42~~ | ~~Não há rota de importação de tema na UI: o motor existe, o caminho de usuário não~~ | **FECHADA em 2026-08-12 pelas PRs #68 e #72** | Quatro rotas na ponte HTTP: `/theme/import/esde/{inspect,apply}` e `/theme/import/package/{inspect,apply}`. `inspect` vem antes de `apply` nas duas: no ES-DE porque a conversão não é fiel e um esquema cuja identidade está na arte vira cinza (`isMonochrome`), e no formato próprio porque importar id já existente SOBRESCREVE (`alreadyInstalled`). Ciclo medido ponta a ponta: criar → editar → salvar → exportar → inspecionar → importar. Ficou marcada como aberta por descuido meu ao fechar as PRs; a correção é de registro, não de código. RetroFE segue sem importador — o formato tem fatia vertical de declarações, não conversor de tema |
| ~~G43~~ | ~~Não existe marcação de jogo ou tema para lançamento pelo Steam Big Picture~~ | **REGISTRADA COM ERRO e CORRIGIDA em 2026-08-12** | A lacuna foi escrita sem medir, a partir da mesma varredura equivocada do registro de métodos do daemon que produziu o diagnóstico errado de G41/G42 — ou seja, cometi o erro DENTRO da correção dele. Medido depois, em ambiente isolado: a cadeia de marcação de JOGO existe e funciona inteira — `game.steam.set` marca, `steam.shortcuts.sync` grava o `shortcuts.vdf` pelo adapter M11 (427 bytes, appId resolvido) e `game.media.publish-steam` publica a arte, exigindo que o jogo esteja marcado e sincronizado. O que de fato faltava era outra coisa, e mais estreita: o gravador suportava só duas formas de `LaunchOptions` — `emulation launch --game-id` e `cloud launch --platform`, ambas DESTINOS. Não havia atalho que abrisse a própria INTERFACE, que é o que torna o Big Picture porta de entrada como em ES-DE, LaunchBox ou RetroFE. Fechado pelo terceiro tipo de atalho (`steam.frontend-shortcut.sync`, `LaunchOptions: desktop ui`), com tabela fechada de modos, preservação de atalhos de jogo/nuvem/terceiros e convergência para no-op |
| ~~G44~~ | ~~Credencial guardada em coleção efêmera do chaveiro some sem aviso, e o produto reporta "não configurado" como se o usuário não tivesse preenchido~~ | **PARCIALMENTE FECHADA em 2026-08-12** | O produto passa a distinguir os três casos: `notConfigured` (nunca preenchido), `stored` (preenchido e presente) e **`vaultVolatile`** (preenchido e o cofre perdeu). Um marcador persistente em `credential-markers-v1.json` guarda apenas id do provedor e instante — nenhum segredo, verificado por teste — e sobrevive a reinícios, o que `_credential_health` não fazia por ser dicionário em memória. Marcador ilegível degrada para `notConfigured`, a mensagem conservadora. O que PERMANECE aberto: a causa de host não é resolvida — o `gnome-keyring-daemon` continua servindo só a coleção `session` enquanto o `kdewallet.kwl` do operador fica de fora, e escolher daemon pelo usuário segue fora de escopo por afetar todas as senhas dele. Também falta a UI mostrar o estado novo com texto acionável: hoje `vaultVolatile` mapeia para `unavailable` na saúde, sem dizer que o cofre é o culpado |
| G45 | Perfil de controle resolvido e nunca lido; instalação de emulador nunca vista concluir | **PARCIALMENTE FECHADA em 2026-08-12 — o índice físico passa a ser LIDO, gravado e desenhado** | Segundo incremento. O que fechou: (a) `steamzero.domain.retroarch_autoconfig` resolve o índice de cada botão LENDO o autoconfig que descreve o pad real — o RetroArch empacota 420 desses arquivos, e cada um é a tabela "posição física → índice" declarada pelo fabricante; sem arquivo para o pad conectado o resultado é NÃO RESOLVIDO e visível, nunca um palpite; (b) o arquivo gerenciado é gravado com marcador `# SteamZero-Managed: true`, de forma atômica e idempotente por conteúdo, jamais sobrescrevendo arquivo sem marcador e jamais tocando o `retroarch.cfg` (que é LIDO para descobrir onde o RetroArch procura perfis); (c) `controlsProfile.autoconfig` publica oito estados e a UI (`ControlsProfileCard`) os desenha, com a regra de que SOMENTE `applied` fica verde. **Correção de medição:** a #77 registrou que "direcionais chegam como eixo nos autoconfigs reais"; com casamento exato de chave (que exclui as linhas `*_label` que contaminaram a contagem original) são 296 arquivos com `input_up_btn` contra 134 com `input_up_axis` — o OPOSTO. O sufixo abstrato de `retropad_key` fica documentado como provisório e é substituído pelo do dispositivo antes de gravar. **Segunda rodada (revisão):** a entrega tinha o writer sem rota de produção — `apply()` só era chamado por teste, então o perfil chegava a `pending-write` e ninguém materializava o arquivo. Fechado por uma ação confirmada (`controls.autoconfig.apply`) exposta na UI. Três correções junto: (a) **brecha de ownership** — a leitura tolerante devolvia `None` tanto para arquivo inexistente quanto para symlink, arquivo grande demais ou ilegível, e quem lia esse `None` concluía "ausente, pode escrever"; um `steamzero.cfg` do usuário nessas formas seria substituído sem que o marcador fosse conferido. Agora só ENOENT autoriza gravar, e a criação é exclusiva (`os.link`), então a recusa é garantia e não checagem com janela; (b) **perfil parcial não é mais gravado** — meio perfil em disco é pior que perfil nenhum, e o critério da entrega é gravar só com tudo resolvido; `partial` virou diagnóstico; (c) **`controlsReadiness` deixou de dar falso verde** — dizia `ready` com perfil salvo e controle plugado, ignorando que o emulador ainda rodava nos padrões dele; agora exige `applied`. **Terceira rodada (revisão):** os quatro P1 restantes tinham uma causa comum — um efeito colateral pendurado num plano VAZIO. Com o CONTEÚDO dentro do plano: a corrida de propriedade (que estava INVERTIDA — reconsultar o alvo antes de gravar desligava a criação exclusiva justamente quando um arquivo estrangeiro aparecia) passou a ser barrada pela precondição de fingerprint; o commit passou a ocorrer só depois de gravar e verificar (e `write-failed`, que era inalcançável, saiu do vocabulário); o plano ficou amarrado a perfil/revisão/orientação/destino, então não dá mais para confirmar A e gravar B; e o rollback virou real (desfazer remove o arquivo criado e restaura o anterior), o que também elimina o `G-NONE` exibido como `G-FULL` no histórico. Quinto: perfil por JOGO deixou de oferecer uma gravação que aplicaria o perfil da PLATAFORMA — o autoconfig é por DISPOSITIVO e não sabe qual jogo roda, então o estado `unsupported-scope` diz que perfil por jogo exigiria remap por jogo, ainda não implementado. **Quarta rodada (revisão):** duas afirmações minhas caíram por medição. (a) A corrida NÃO estava fechada: `_revalidate_preconditions` roda uma vez no início do `apply` e ainda há staging e backup antes da escrita; um intruso criado nessa janela era copiado para o backup e sobrescrito, com a operação retornando `ok`. Corrigido pondo a política no plano — precondição AUSENTE ⇒ criação exclusiva (`os.link`), alvo existente ⇒ fingerprint reconferido imediatamente antes de publicar; dois testes cobrem a janela. (b) `preserve_existing_dir` estava global em `_apply_actions`, mudando em silêncio a política de todas as capacidades; virou `foreign_dir`, propriedade do plano, com o padrão restaurando o comportamento histórico. (c) **"Abrir o RetroArch uma vez" NÃO destrava a gravação** — eu estava errado ao pedir isso: o Flatpak 1.22.2 declara `joypad_autoconfig_dir = /app/share/libretro/autoconfig`, caminho interno do sandbox que não existe no host, e procura perfis em `<dir>/<input_joypad_driver>/` (`udev`, 420 arquivos; a raiz tem ZERO — o código gravava na raiz). `--appendconfig` funciona em tempo de lançamento, mas `config_save_on_exit=true` persiste o valor no arquivo usado, então aplicá-lo ao `retroarch.cfg` do usuário seria editá-lo permanentemente. Evidência medida em `docs/09-operations/evidence/2026-08-13-retroarch-autoconfig/`. **Quinta rodada — a integração Flatpak foi implementada e PROVADA.** O RetroArch é lançado com `--appendconfig` apontando `joypad_autoconfig_dir` para uma árvore gerenciada do SteamZero; o overlay também desliga `config_save_on_exit`, sem o que o RetroArch gravaria a injeção dentro do `retroarch.cfg` do usuário ao sair (verificado: o config real sai byte a byte idêntico). Perfil e overlay são gravados na MESMA transação — um sem o outro não vale. **Prova ponta a ponta** com joypad virtual (`uinput`) e o perfil gerado pelo código de produção: sem overlay o RetroArch reporta `[Autoconf] 8BitDo SF30 2.4G` (perfil empacotado); com overlay reporta `[Autoconf] SteamZero Virtual Pad` (o NOSSO). Como o diretório passou a ser nosso, a mudança ampla de núcleo (`preserve_existing_dir`) deixou de ser necessária e foi REVERTIDA — do núcleo permanece só a correção da corrida. **Sexta rodada (revisão):** a prova A/B validava o mecanismo do RetroArch, não a integração do produto — o `--appendconfig` tinha sido injetado só no `ComponentLifecycle`, e a tela de emulação monta o argv em `EmulationController`, então "Abrir emulador" e "Jogar" subiam sem o perfil. A composição virou ponto único, chaveada pela REF do Flatpak (não pelo id do adapter, que deixaria os cores libretro — ou seja, os jogos — de fora). Mais três defeitos da mesma classe, todos reproduzidos: a janela de ATUALIZAÇÃO ainda sobrescrevia intruso (fechada com `renameat2`/`RENAME_EXCHANGE`, com desfazimento verificado); o rollback removia arquivo estrangeiro porque `expectHash` saía `None` para ações `write`, PULANDO um guard que já existia; e — quarta instância, que a revisão não listou — o `restore` do rollback sobrescrevia o alvo sem conferir identidade. O que PERMANECE ABERTO: (i) **nada foi gravado no host** — o writer só rodou em diretório temporário, por decisão desta sessão; (ii) o perfil vale apenas nas sessões lançadas PELO SteamZero (a UI diz isso); um RetroArch aberto direto pelo menu do sistema segue lendo o diretório interno do Flatpak. Nas sessões do SteamZero, `config_save_on_exit` fica desligado, então ajustes feitos no menu do emulador não persistem — preço de não reescrever a configuração do usuário; (iii) o pad interno deste Steam Deck (10462:4613) não tem autoconfig empacotado, então ele cai em `no-autoconfig` — falta decidir a fonte de índice para pads sem arquivo; (iv) nenhuma instalação de emulador foi vista concluir no host (só o fluxo isolado, 274 testes verdes); (v) não há prova física de que o RetroArch aplica o arquivo gerado — isso exige o operador |
